owner: wearecordice
repo: status

# Every address a user or a client actually depends on, checked from outside.
#
# The point of this repository is that none of it runs on our infrastructure:
# the checks run on GitHub's machines and the page is served by GitHub Pages, so
# a total outage of our server still leaves a working page saying so. A status
# page hosted next to the thing it watches goes quiet exactly when it matters.
sites:
  - name: Web client
    url: https://app.rhmt.online/
    icon: https://app.rhmt.online/favicon.ico

  - name: API
    url: https://app.rhmt.online/api/
    # The discovery document. It touches configuration, the database and the
    # file service's address, so a 200 here means rather more than a static page.
    expectedStatusCodes:
      - 200

  - name: Events
    # Not the socket itself: a WebSocket handshake cannot be made over HTTP/2,
    # which is what this monitor speaks, and the proxy would answer 502 — the
    # same 502 a dead service returns. So the service answers this path itself,
    # with a body no other part of the stack would produce.
    url: https://app.rhmt.online/ws/health
    __dangerous__body_down_if_text_missing: ok

  - name: File service
    url: https://app.rhmt.online/autumn/

  - name: Website
    url: https://cordice-site.pages.dev/

  - name: Developer documentation
    url: https://developers.rhmt.online/

status-website:
  cname: status.rhmt.online
  name: Cordice Status
  theme: dark
  # The landing page's palette, so the two read as one product. See the file
  # for why the up/degraded/down colours are not the brand's.
  # Inline rather than a file. A themeUrl pointing at our own host makes the
  # site generator crawl that path as if it were a page, which creates a
  # directory where the stylesheet was going to be written and fails the build.
  # The source of these values, with the reasoning, is assets/cordice.css.
  css: |
    :root {
      --body-background-color: #110e17;
      --body-text-color: #ede9f5;
      --card-background-color: #221c2e;
      --nav-background-color: #191523;
      --nav-border-bottom-color: #322a46;
      --nav-current-border-bottom-color: #00d8ff;
      --card-border-color: #322a46;
      --up-border-left-color: #22c55e;
      --degraded-border-left-color: #f5a623;
      --down-border-left-color: #ef4444;
      --down-background-color: #3b1620;
      --tag-color: #110e17;
      --tag-up-background-color: #22c55e;
      --tag-degraded-background-color: #f5a623;
      --tag-down-background-color: #ef4444;
      --change-background-color: #f5a623;
      --error-button-border-color: #00d8ff;
      --error-button-background-color: #00d8ff;
      --error-button-color: #110e17;
      --submit-button-border-color: #00d8ff;
      --submit-button-background-color: #00d8ff;
      --submit-button-color: #110e17;
      --graph-opacity: 0.9;
    }
    .card {
      border-radius: 12px;
    }
    a {
      color: #00d8ff;
    }
    .intro {
      max-width: 62ch;
      line-height: 1.6;
    }
  logoUrl: https://cordice-site.pages.dev/favicon.svg
  faviconSvg: https://cordice-site.pages.dev/favicon.svg
  introTitle: "Whether Cordice is working, checked from outside our own infrastructure."
  introMessage: >-
    Every service is probed every five minutes from GitHub's machines, and this
    page is served by GitHub — so if our servers go down, this page stays up and
    says so. History and incidents are public.
  navbar:
    - title: Status
      href: /
    - title: Cordice
      href: https://cordice-site.pages.dev
    - title: Developers
      href: https://developers.rhmt.online
    - title: Source
      href: https://github.com/wearecordice/status

# An outage opens an issue and a recovery closes it, so the incident history is
# the issue history and nobody has to remember to write one.
assignees:
  - schadomia
